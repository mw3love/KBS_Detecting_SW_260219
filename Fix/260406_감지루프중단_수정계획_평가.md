# 감지루프중단 수정계획 Phase 1 수정 평가

**평가일**: 2026-04-06
**에이전트**: eval-plan (evaluate 모드)
**평가 대상 커밋**: 314551e — "fix: DIAG 블록 try-except 보호 + health check _last_detection_time 이동"
**원본 계획서**: Fix/260405_감지루프중단_수정계획.md

---

## 수정 내용 요약

| # | 파일 | 수정 위치 | 변경 내용 | 관련 영역 |
|---|------|-----------|-----------|-----------|
| 1 | main_window.py | `_run_detection()` L284 | `if self._detection_count % 150 == 0:` 내부 전체를 `try-except`로 감싸기 | A, E |
| 2 | main_window.py | `_run_detection()` L417 | `self._last_detection_time = time.time()` 를 DIAG 블록 이후·감지 try 이전으로 이동 | E |
| 3 | main_window.py | SYSTEM-HB 로그 | `threads=py:%d/os:%d` 항목 추가 (`threading.active_count()` + `psutil.Process().num_threads()`) | E |
| 4 | main_window.py | 파일 상단 | `import traceback` 추가 | B |

---

## 계획 대비 구현 일치도

| 계획서 항목 | 구현 여부 | 차이점 |
|------------|-----------|--------|
| SYSTEM-HB에 `threads=py:N/os:N` 추가 | 완료 | 계획서 코드와 완전 일치 |
| `_last_detection_time` DIAG 블록 이후·try 이전으로 이동 | 완료 | 계획서가 지정한 정확한 위치에 배치 |
| DIAG 블록 전체를 독립 try-except로 감싸기 + 동일 예외 억제 | 완료 | 계획서 코드와 완전 일치 |
| `import traceback` 추가 | 완료 | 파일 상단 표준 import 블록에 정상 추가 |
| L283의 기존 `_last_detection_time` 갱신 코드 제거 | 완료 | diff 확인 — 기존 라인 삭제됨 |

---

## 발견 사항

### Critical

발견 없음.

### High

발견 없음.

### Medium

| # | 영역 | 파일 | 위치(함수/라인) | 설명 |
|---|------|------|----------------|------|
| M1 | E | main_window.py | `_run_detection()` L409 | `_diag_last_error_type` 속성이 `__init__`에 초기화되어 있지 않다. `getattr(self, "_diag_last_error_type", None)` 방어 코드가 있어 실제 오류는 없지만, 프로그램 시작 직후 첫 DIAG 예외 발생 시 `hasattr` 없이 `getattr` 기본값으로 처리되는 방식이다. 명시적 초기화(`self._diag_last_error_type: Optional[str] = None`)가 없어 속성 존재 여부가 불명확하다. 기능 문제는 없으나 클래스 인터페이스 일관성이 낮다. |
| M2 | E | main_window.py | `_run_detection()` L285~L308 | `import psutil as _psutil` 가 `if self._detection_count % 150 == 0:` 블록 안에 있어 매 30초마다 import 시도가 반복된다. Python은 sys.modules 캐시로 실제 IO 비용은 없으나, 예외 구분을 위한 `_diag_last_error_type` 억제 로직과 조합할 때 첫 번째 `ImportError`(psutil 미설치 시)가 DIAG 전체를 차단한다는 점을 인지해야 한다. 현재 psutil은 설치된 패키지(`CLAUDE.md` 확인)이므로 운영 환경에서 `ImportError` 발생 가능성은 없다. |

### Low

| # | 영역 | 파일 | 위치(함수/라인) | 설명 |
|---|------|------|----------------|------|
| L1 | B | main_window.py | L14 | `import traceback`이 파일 상단 표준 라이브러리 블록(`import os`, `import sys` 등)에 추가되었다. 위치는 적절하나 `from typing import Optional` 보다 앞 줄에 있어 알파벳 정렬 순서(`sys` 다음에 `time`, `traceback` 순)를 따른다. 기능 문제는 없으며 Python 관행에 부합한다. |
| L2 | A | main_window.py | `_run_detection()` L407~L415 | except 블록에서 `_log.error()`를 사용한다. DIAG 블록은 진단 로그이므로 예외 발생 자체가 감지 실패를 의미하지 않는다는 점에서 `_log.warning()`도 고려 가능하다. 다만 traceback을 동반하므로 `error`가 사용자에게 주의를 더 효과적으로 줄 수 있어 현재 선택이 합당하다. |

---

## 영향 범위 확인

수정이 영향을 미치는 코드 중 함께 수정되지 않은 부분:

| # | 파일 | 위치 | 영향 내용 | 문제 여부 |
|---|------|------|-----------|-----------|
| 1 | main_window.py | `_update_summary()` L623~L656 (health check 블록) | `_last_detection_time` 위치 이동으로 health check 동작이 의도한 대로 변경됨. 이제 DIAG 예외 발생 시 `_last_detection_time`이 갱신되지 않으므로 이론상 5초 임계값 초과 시 health check 경보 가능. 단, DIAG 예외는 150콜 중 1콜에만 발생하고 나머지 149콜에서 정상 갱신되므로 실질적 오탐 없음. 계획서 "효과 범위 주의" 항목이 이를 명시하고 있어 설계 의도에 부합. | 문제 없음 |

---

## 부작용 분석

새로 도입된 위험 없음.

- DIAG 블록 예외 발생 시 `_log.error()`가 30초마다 반복 호출될 수 있으나, 동일 예외 타입 억제 로직(`_diag_last_error_type` 비교)으로 로그 폭풍은 방지됨.
- `self._last_detection_time = time.time()`의 이동으로 기존 대비 호출 위치가 약 0~수ms 늦어지나 health check 임계값(5초)에 비해 무시 가능한 수준.

---

## 종합 평가

- **수정 품질**: 상 — 계획서의 모든 항목이 정확히 구현되었으며, 감지 루프 안정성 원칙(`core/CLAUDE.md`)을 충실히 따른다.
- **잔여 Critical**: 0건
- **잔여 High**: 0건
- **계획 대비 일치도**: 완전 일치 — 계획서 Phase 1의 4개 항목이 모두 완료됨.

---

## 권장 조치

### 추가 수정 필요 (Critical/High)

없음.

### 검증 방법

1. **DIAG 예외 억제 로직 확인**: `_detector._audio_level_states`에 임시로 잘못된 값을 넣거나 psutil 속성에 오류를 유발해 DIAG 블록에서 예외가 발생할 때 `DIAG 로깅 오류 (감지 계속)` 로그가 출력되고, 이후 감지 로직(video_rois, audio_rois 처리)이 정상 실행되는지 확인.
2. **`_last_detection_time` 위치 확인**: `git diff HEAD~1 HEAD kbs_monitor/ui/main_window.py | grep _last_detection_time`으로 기존 L283 위치에 갱신 코드가 없고 감지 try 바로 앞(L417)에만 있는지 확인.
3. **SYSTEM-HB 스레드 수 로그 확인**: 30초 대기 후 로그 파일에서 `threads=py:N/os:N` 항목이 출력되는지 확인.
4. **장기 실행 검증 (Phase 4)**: 24시간 이상 실행 후 다음 패턴이 없는지 확인:
   - DIAG-AUDIO 소멸 패턴 재발 (발생 시 `DIAG 로깅 오류` 로그로 대체되어야 함)
   - `DIAG 로깅 오류 반복 (감지 계속)` 로그 — 실제 예외 유형 파악 후 Phase 2로 이행
