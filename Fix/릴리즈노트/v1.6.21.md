# v1.6.21 릴리즈 노트

**릴리즈 날짜**: 2026-04-12

## 변경사항 요약

### 버그 수정
- `exit_prep_minutes` 3파일 동기화 누락 수정 (`default_config.json` 변경이 `config_manager.py` DEFAULT_CONFIG·`kbs_config.json`에 미반영)
- 자동녹화 `_MAX_RECORD_FRAMES` 상수 오산정 수정 — `output_fps: 30` 설정 시 3000프레임 ÷ 30fps = 100초 만에 강제 종료되던 문제. 9000프레임(5분 상한)으로 수정

### 개선
- `settings.local.json` allowedTools 정리 — 템플릿 셋업 디버깅 세션에서 추가된 임시 항목 20개 제거 (타 사용자 경로, 날짜 하드코딩 명령어 등)

## 커밋 이력

이번 버전에 포함된 변경사항 (/simplify 실행 결과 적용):
- `kbs_monitor/utils/config_manager.py` — group1 `exit_prep_minutes` 30 → 180
- `kbs_monitor/config/kbs_config.json` — group1 `exit_prep_minutes` 30 → 180
- `kbs_monitor/core/auto_recorder.py` — `_MAX_RECORD_FRAMES` 3000 → 9000
- `.claude/settings.local.json` — 임시 allowedTools 항목 20개 제거

## 정적 검증 결과

/simplify 실행 (2026-04-12):
- 코드 재사용 검토: exit_prep_minutes 3파일 동기화 규칙 위반 발견 → 수정
- 효율성 검토: _MAX_RECORD_FRAMES 상수 FPS 변경 미반영 발견 → 수정
- 코드 품질 검토: settings.local.json 임시 항목 잔류 → 정리
